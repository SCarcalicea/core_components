import 'package:flutter/material.dart';

class ExpandedBodyView extends StatelessWidget {
  const ExpandedBodyView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Add any kind of widgets here."),
      ),
    );
  }
}
